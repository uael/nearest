# Ignore everything
*

# Git
# !.git and !.git/** are needed for release-plz, which uses the `ignore` crate
# to copy the repo to a temp dir. Without these, the deny-all `*` pattern
# causes .git internals to be skipped, breaking the copy. (release-plz#1961)
!.git
!.git/**
!.gitignore

# Project
!README.md
!CHANGELOG.md
!LICENSE-*
!deny.toml
!release-plz.toml
!.github/
!.github/workflows/
!.github/**/*.yml

# Rust
!Cargo.toml
!Cargo.lock
!rust-toolchain
!rustfmt.toml
!.cargo/
!.cargo/config.toml
!nearest/
!nearest/benches/
!nearest/examples/
!nearest/src/
!nearest/tests/
!nearest/tests/compile-fail/
!nearest/tests/compile-fail/*.stderr
!nearest/**/*.rs
!nearest-derive/
!nearest-derive/src/
!nearest-derive/**/*.rs

# Rust (xtask)
!xtask/
!xtask/Cargo.toml
!xtask/src/
!xtask/**/*.rs

# Claude
!CLAUDE.md
!.claude/
!.claude/settings.json
!.claude/commands/
!.claude/**/*.md
